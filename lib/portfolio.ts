// Personal "book" / context injected into every research prompt via {{portfolio_context}}.
// Edit the text below to change what the model assumes about your positioning.
export const DEFAULT_PORTFOLIO_CONTEXT = `- I am already heavily exposed to memory names (Micron / DRAM / SanDisk). Do not merely confirm my current book — actively challenge concentration risk and surface diversifiers across the AI-infrastructure stack.`;
